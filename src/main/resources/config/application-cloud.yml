ghtk:
  api_token: ${GHTK_API_TOKEN}
  url_service: ${GHTK_API_URL}

spring:
  jpa.hibernate.ddl-auto: update
  datasource:
    url: jdbc:mysql://${RDS_HOSTNAME}:${RDS_PORT}/${RDS_DB_NAME}?useUnicode=yes&characterEncoding=UTF-8
    username: ${RDS_USERNAME}
    password: ${RDS_PASSWORD}